{
  "name": "aniside",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "clients/*",
    "servers/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.4.3"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  }
}